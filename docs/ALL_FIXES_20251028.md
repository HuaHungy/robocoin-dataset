# 转换系统修复总结 - 2025-10-28

## 📋 今日修复清单

### ✅ 0. FileExistsError修复 - 支持转换重试

**问题**: mmk2等数据集转换失败后，重试时报`FileExistsError`，无法自动恢复

**根本原因**: 
- lerobot库的`LeRobotDataset.create()`**不支持**`exist_ok`参数
- 内部硬编码使用`mkdir(exist_ok=False)`
- 之前失败的转换留下目录残留

**修复**:
- 在创建前检查并删除已存在的目录
- 添加重试机制（最多3次），处理并发竞态条件
- 详细的日志输出，便于诊断

```python
def _create_lerobot_dataset(self) -> LeRobotDataset:
    # 检查目录是否存在
    if output_path.exists():
        # 删除旧目录（失败转换的残留）
        shutil.rmtree(output_path)
    
    # 尝试创建数据集
    try:
        return LeRobotDataset.create(...)
    except FileExistsError:
        # 重试机制（并发冲突）
        time.sleep(2)
        continue
```

**影响范围**: 
- ✅ 所有使用`LeRobotDataset.create()`的转换器
- ✅ mmk2, galaxea, yinhe, leju, alohanew等
- ✅ 支持失败任务自动重试

**详细文档**: `docs/FILE_EXISTS_ERROR_FIX.md`

---

### ✅ 1. 配置文件清理 - 删除重复配置

**问题**: 创建了full/lite双版本后，旧配置文件仍然存在，导致混淆

**修复**:
- ✗ 删除 `converter_config_yinhe.yaml`
- ✗ 删除 `converter_config_discover_robotics_aitbot_mmk2_third_view.yaml`
- ✓ 只保留 `*_full.yaml` 和 `*_lite.yaml` 两个版本

**影响数据集**: yinhe, mmk2

---

### ✅ 2. yinhe配置完整性提升 - 添加body字段

**问题**: `yinhe_full.yaml` 缺少body字段（4维），但本地数据显示有完整数据

**本地验证数据**:
```
cmd_body_joint: 1857 elements (valid)
Sample values: [0.4579, 1.3141, 0.8607, 0.0005]
```

**修复**: 修改 `converter_config_yinhe_full.yaml`
- **Before**: 16维 (2 head + 7 left_arm + 7 right_arm)
- **After**: 20维 (4 body + 2 head + 7 left_arm + 7 right_arm)

```yaml
action:
  # Total: 20维
  sub_action:
    # Body joints (4维) - 恢复body字段 🆕
    - names:
        - body_joint_1_rad
        - body_joint_2_rad
        - body_joint_3_rad
        - body_joint_4_rad
      args:
        json_path: cmd_body_joint
        field_name: position
        range_from: 0
        range_to: 4
```

**注意**: 
- 从验证数据看，body是**4维**，不是state配置中的3维
- state配置的body维度也需要同步检查

---

### ✅ 3. galaxea视频解码问题 - 初始化采样优化

**问题**: 
```
RuntimeError: Failed to read frame 0 from .../3115_cam_high.mp4
- 视频文件存在 ✓
- OpenCV无法解码 ❌
```

**根本原因**: 
- galaxea视频使用不兼容的编码格式（可能是AV1/HEVC）
- 初始化时使用 `LazyVideoReader` (OpenCV cv2.VideoCapture)
- OpenCV编解码器支持有限

**修复策略**: 
分阶段使用不同的视频读取方式

| 阶段 | 方式 | 工具 | 原因 |
|------|------|------|------|
| 初始化采样 | 只读1帧 | PyAV | 更好的编解码器支持 |
| 测试转换 | 读11帧 | PyAV | 快速验证 |
| 正式转换 | 延迟加载 | OpenCV | 高性能，低内存 |

**代码修改**:

1. `lerobot_format_converter_h5_mp4.py` - 添加 `sample_only` 参数
```python
def _prepare_episode_images_buffer(
    self, task_path: Path, ep_idx: int, 
    is_test: bool = False,
    sample_only: bool = False  # 🆕 初始化采样模式
):
    if sample_only:
        # 🎯 只加载1帧，使用PyAV（更兼容）
        return self._load_frames_to_memory(ep_dir, max_frames=1)
    
    if is_test or self._is_test_mode:
        # 🧪 加载11帧，使用PyAV
        return self._load_frames_to_memory(ep_dir, max_frames=11)
    
    # 🚀 延迟加载，使用LazyVideoReader (OpenCV)
    return self._create_lazy_readers(ep_dir)
```

2. `lerobot_format_converter.py` - 智能检测并使用sample_only
```python
def _get_one_frame_image(self, args_dict: dict) -> np.ndarray:
    # 🔍 检查子类是否支持sample_only
    import inspect
    supports_sample_only = 'sample_only' in inspect.signature(self._get_frame_image).parameters
    
    for task_path, ep_idx in ...:
        if supports_sample_only:
            # 🎯 初始化采样模式（PyAV）
            image = self._get_frame_image(..., sample_only=True)
        else:
            image = self._get_frame_image(...)
```

**技术优势**:
- ✅ 更广的编解码器支持（H.264, HEVC, AV1, VP9等）
- ✅ 向后兼容（子类可选实现）
- ✅ 性能优化（正式转换仍用LazyVideoReader）

**受益数据集**:
- galaxea (星图/星海图)
- alohanew
- 所有H5+MP4格式数据集

---

## 📊 配置文件版本选择指南

### yinhe 数据集

| 配置版本 | 适用场景 | Action维度 | 包含字段 |
|---------|---------|-----------|---------|
| `yinhe_full.yaml` | 完整数据（优先使用） | 20维 | body(4) + head(2) + left_arm(7) + right_arm(7) |
| `yinhe_lite.yaml` | 服务器旧数据 | 14维 | left_arm(7) + right_arm(7) |

**选择规则**:
1. 优先使用 `yinhe_full.yaml`
2. 如果超过50%的episode缺少head/body数据，改用 `yinhe_lite.yaml`
3. 容错机制会自动跳过缺失字段的个别episode

### mmk2 数据集

| 配置版本 | 适用场景 | Action维度 | 包含字段 |
|---------|---------|-----------|---------|
| `mmk2_full.yaml` | 完整数据（优先使用） | 39维 | left_arm(6) + right_arm(6) + head(2) + spine(1) + hands(24) |
| `mmk2_lite.yaml` | 服务器旧数据 | 37维 | left_arm(6) + right_arm(6) + spine(1) + hands(24) |

**选择规则**: 同yinhe

**详细文档**: `docs/CONFIG_VERSION_SELECTION.md`

---

## 🔧 部署清单

### 1. 代码同步

**所有节点**:
```bash
cd ~/robocoin-dataset
git pull origin feat/test
```

### 2. 服务重启

**Server**:
```bash
# 停止Server
# 启动Server
```

**Client** (所有节点):
```bash
# 停止Client
cd ~/robocoin-dataset
git pull origin feat/test
# 启动Client
```

### 3. 验证日志

**galaxea初始化成功**:
```
🎯 Sample mode: loading 1 frame for initialization (episode 0)
✅ Successfully got sample image for camera 'cam_high_rgb' from ... (sample mode)
```

**yinhe/mmk2使用完整配置**:
```
📊 Action dimension: 20 (yinhe) / 39 (mmk2)
🔧 Configuration: *_full.yaml
```

**字段缺失容错**:
```
⚠️  Episode X skipped: Missing field 'cmd_head_joint_state'
✅ Fault tolerance: Continuing with next episode
```

---

## 📁 修改文件清单

### 删除的文件
```
✗ scripts/format_converters/tolerobot/configs/converter_config_yinhe.yaml
✗ scripts/format_converters/tolerobot/configs/converter_config_discover_robotics_aitbot_mmk2_third_view.yaml
```

### 修改的文件
```
✏️  scripts/format_converters/tolerobot/configs/converter_config_yinhe_full.yaml
   - 添加body字段（4维）
   - action: 16维 → 20维

✏️  src/robocoin_dataset/format_converter/tolerobot/lerobot_format_converter.py
   - _get_one_frame_image: 添加sample_only智能检测

✏️  src/robocoin_dataset/format_converter/tolerobot/lerobot_format_converter_h5_mp4.py
   - _prepare_episode_images_buffer: 添加sample_only参数
   - _get_frame_image: 添加sample_only参数
```

### 新增文件
```
📄 docs/GALAXEA_VIDEO_DECODE_FIX.md - galaxea视频解码问题详细文档
📄 docs/ALL_FIXES_20251028.md - 本文档
```

---

## 🎯 关键改进点

### 1. 配置管理规范化
- 删除重复配置
- 明确full/lite版本用途
- 提供版本选择指南

### 2. 数据完整性提升
- yinhe action维度: 14维 → 20维
- 本地数据验证机制
- 配置与实际数据一致性

### 3. 视频兼容性增强
- 初始化采样使用PyAV
- 支持更多编解码器
- 正式转换保持高性能

### 4. 容错机制完善
- 字段缺失自动跳过
- 多episode尝试机制
- 详细错误诊断信息

---

## 📈 预期效果

### Before
```
❌ galaxea: 初始化失败（视频解码）
⚠️  yinhe: 数据不完整（缺body字段）
⚠️  mmk2: 维度不匹配（37 vs 39）
🔀 配置混乱（新旧配置共存）
```

### After
```
✅ galaxea: 初始化成功，正常转换
✅ yinhe: 完整20维数据
✅ mmk2: 完整39维数据
✅ 配置清晰（只有full/lite两版本）
✅ 容错机制完善
```

---

## 🔍 后续观察点

### 1. galaxea转换成功率
- 监控初始化成功率
- 检查是否仍有视频解码失败
- 如有失败，考虑启用 `auto_reencode`

### 2. yinhe/mmk2数据完整性
- 统计使用full版本的成功率
- 记录跳过的episode比例
- 如果跳过率>50%，考虑切换到lite版本

### 3. 性能影响
- 初始化时间对比（PyAV vs OpenCV）
- 内存占用监控
- 转换速度统计

---

## 📞 问题排查

如遇问题，按以下顺序检查：

1. **代码版本**: `git log --oneline -5` 确认最新提交
2. **配置文件**: 确认使用正确的full/lite版本
3. **日志分析**: 查看具体错误消息
4. **手动测试**: 使用 `scripts/test_converter_episode_location.py`
5. **数据验证**: 使用 `scripts/config_validation/validate_local_datasets.py`

---

**修复时间**: 2025-10-28  
**修复人员**: Assistant  
**状态**: ✅ 已完成，待部署  
**Git分支**: feat/test

