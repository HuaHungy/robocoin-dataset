# 修复：写入文件前确保目录存在

**日期**: 2025-10-27  
**问题**: `FileNotFoundError` when writing `invalid_episodes.txt`  
**影响**: H5+JPG, MMK2 converters

---

## 🐛 问题

### 错误信息
```
FileNotFoundError: [Errno 2] No such file or directory: 
'/mnt/nas/synnas/docker2/robocoin-datasets-test/ruantong_a2d_capture_item/invalid_episodes.txt'
```

### 根本原因
在 `_prevalidate_files()` 方法中尝试写入 `invalid_episodes.txt` 时，输出目录 `self.output_path` 还不存在。

**时间线**：
```
1. __init__() 开始
2. _prevalidate_files() 执行
3. 发现invalid episodes
4. 尝试写入 invalid_episodes.txt
5. ❌ 错误：输出目录不存在
```

---

## ✅ 修复

### 修改的文件

#### 1. lerobot_format_converter_h5_jpg.py
**行数**: 246

**修改前**：
```python
if self._invalid_episodes:
    invalid_episodes_file = self.output_path / "invalid_episodes.txt"
    with open(invalid_episodes_file, 'w', encoding='utf-8') as f:
```

**修改后**：
```python
if self._invalid_episodes:
    # 🆕 确保输出目录存在
    self.output_path.mkdir(parents=True, exist_ok=True)
    
    invalid_episodes_file = self.output_path / "invalid_episodes.txt"
    with open(invalid_episodes_file, 'w', encoding='utf-8') as f:
```

#### 2. lerobot_format_converter_mmk2.py
**行数**: 357

**修改前**：
```python
if self._invalid_episodes:
    invalid_episodes_file = Path(output_path) / "invalid_episodes.txt"
    try:
        from datetime import datetime
        with open(invalid_episodes_file, 'w', encoding='utf-8') as f:
```

**修改后**：
```python
if self._invalid_episodes:
    # 🆕 确保输出目录存在
    Path(output_path).mkdir(parents=True, exist_ok=True)
    
    invalid_episodes_file = Path(output_path) / "invalid_episodes.txt"
    try:
        from datetime import datetime
        with open(invalid_episodes_file, 'w', encoding='utf-8') as f:
```

---

## 📊 影响范围

- ✅ H5+JPG converter (`lerobot_format_converter_h5_jpg.py`)
- ✅ MMK2 converter (`lerobot_format_converter_mmk2.py`)
- ✅ 其他converters无此问题（已检查）

---

## 🚀 部署

**立即生效**：
1. 同步代码到所有Client机器
2. 重启Clients（Server无需重启）

```bash
# 在每台Client机器上
cd ~/robocoin-dataset
git pull origin feat/test

# 重启clients
Ctrl+C
python scripts/format_converters/tolerobot/multi_client.py \
    --host 172.16.13.140 --port 8769 --num-clients 4
```

---

## ✅ 验证

修复后，转换任务应该不再报 `FileNotFoundError` 错误。

如果数据集有invalid episodes，会看到：
- ✅ 成功创建 `invalid_episodes.txt` 文件
- ✅ 文件包含invalid episodes列表
- ✅ 转换继续进行（跳过invalid episodes）

---

**修复状态**: ✅ 已完成  
**下一步**: 同步代码到Client机器并重启

